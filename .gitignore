# Dependencies — must never be committed; platform-specific binaries and
# executable bits do not survive a commit from one OS to another.
node_modules/

# Build output — regenerated by `npm run build` on deploy
dist/

# Local env / tooling
.env
.env.*
.vercel
.DS_Store
*.local
npm-debug.log*
yarn-error.log*
